# MAY2026-Team-103 — SSCMS Frontend

## Running the app

This is a static site, so any static file server works. From the repo root:

```bash
cd Frontend
npx serve -l 5500 .
```

Open http://localhost:5500
